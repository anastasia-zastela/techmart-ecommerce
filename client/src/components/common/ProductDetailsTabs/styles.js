import {makeStyles} from "@material-ui/core";

export default makeStyles(() => ({
    descriptions:{
        textAlign:"center"
    },
    productDetailsWrapper:{
        width: "100%",
        margin: "0 auto",
    }
}))