import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles((theme) => ({
  cardWrapper: {
    width: '22%',
    marginBottom: '30px',
  },
  cardMediaWrapper: {
    height: '140px',
  },
  cardLink: {
    textDecoration: 'none',
  },
  cardTitle: {
    color: '#000',
  },
  cardButton: {
    width: '100%',
  },
}));
